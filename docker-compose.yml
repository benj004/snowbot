services:
  snowbot:
    container_name: snowbot
    build: .
    restart: unless-stopped
    env_file: .env
    working_dir: /app
    volumes:
      - .:/app
    # NEW: Optional - add shared memory for Chrome stability
    shm_size: '2gb'
